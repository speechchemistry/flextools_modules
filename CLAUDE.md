# CLAUDE.md

Claude Code reads `CLAUDE.md` rather than `AGENTS.md`, so this file exists only to load the latter.
Keep it that way: `AGENTS.md` is the single, agent-agnostic source of truth, and no guidance should
be added here.

@AGENTS.md
